"use strict";
// 
function make_shirt(size, printMessage) {
    console.log(` you selected ${size} size shirt with the printMessage "${printMessage}"on it.`);
}
//calling the function:
make_shirt("medium", "beleive In Yourself");
